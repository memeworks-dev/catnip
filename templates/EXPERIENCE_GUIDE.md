# EXPERIENCE_GUIDE

<!-- Placeholder (claude.md §16A). The owner fills this in later. -->

This is the context that guides a coding agent when generating a **custom**
Catnip experience: how to structure a toy, which Catnip hooks are mandatory
(spend-cap reservation, per-visitor quota, input/output moderation, generation
job submission, share-card generation, analytics — §16.7), and the house taste.

The custom-experience builder is a deliberate placeholder in v1 — the only
experience that ships is the meme-booth template. When the builder is built, it
reads this guide plus `blocks/` and produces a toy that plugs into the same
multi-tenant rendering, metering, moderation, virality, and analytics layers.

> **TODO:** write the guide.
