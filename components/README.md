# /components

Shared React components for the dashboard and the public toys.

- `graceful-state.tsx` — the single branded fallback every toy failure path
  resolves to (claude.md §12, hard rule #5).

To be added as features are built: toy template UI (upload, consent, Turnstile,
result, share card + share buttons), dashboard widgets (live usage, spend-vs-cap,
free-runs-remaining), and a cookie-consent banner (§14).
